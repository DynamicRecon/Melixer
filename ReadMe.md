#Melixer

##Description
	A mobile web app that allows someone to scan into a textbox that gets the book info through Google API and immediatly loads it into a 
database backend in Django. The frontend is coded in reactjs.

##Requirements
	See Requirements.txt
